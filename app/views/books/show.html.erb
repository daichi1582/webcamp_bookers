<p class="notice"><%= notice %></p>


<table>
    <tr>
        <th>title:</th>
        <td><%= @book.title %></td>
    </tr>
    <tr>
        <th>body:</th>
        <td><%= @book.body %></td>
    </tr>
</table>

<%= link_to 'Edit', edit_book_path(@book) %>
<%= link_to "Back", books_path %>